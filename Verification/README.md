# Magic Mouse verification

Run `bash Verification/run.sh` from the repository root on macOS with Xcode Command Line Tools. Set `SDKROOT` to a compatible SDK if needed.

The checks exercise gesture recognition and configuration using the real app sources without launching the app, posting input, or changing permissions. Physical gestures and native macOS conflicts require a separate hands-on check.
